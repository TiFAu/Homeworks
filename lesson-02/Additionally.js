function dataTypeChecking (variable){
                                        var timeNow = new Date ();
                                        Number ( variable ) ? console.log(timeNow.toLocaleString()) : console.log("Неверный тип данных")                
                                    }

